name: Reusable Chrome Extension CD

on:
  workflow_call:
    inputs:
      extension-dir:
        description: 'Directory containing extension files'
        required: false
        type: string
        default: 'extension'
      build-command:
        description: 'Optional shell command that builds the extension (e.g. "npm run build" or "cd extension && npm ci && npm run build"). Required when extension-dir is a build output such as dist/.'
        required: false
        type: string
        default: ''
      node-version:
        description: 'Node.js version'
        required: false
        type: string
        default: '20'
      publish-target:
        description: 'Publish target (default, trustedTesters)'
        required: false
        type: string
        default: 'default'
      auto-publish:
        description: 'Auto-publish after upload'
        required: false
        type: string
        default: 'true'
      runner-type:
        description: 'Runner type to use'
        required: false
        type: string
        default: 'ubuntu-latest'
    secrets:
      chrome-extension-id:
        description: 'Chrome Web Store extension ID'
        required: true
      chrome-client-id:
        description: 'Chrome Web Store API client ID'
        required: true
      chrome-client-secret:
        description: 'Chrome Web Store API client secret'
        required: true
      chrome-refresh-token:
        description: 'Chrome Web Store API refresh token'
        required: true

jobs:
  cd:
    name: Package & Release
    runs-on: ${{ inputs.runner-type }}
    timeout-minutes: 30
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ (hashFiles('package-lock.json') != '' || hashFiles('npm-shrinkwrap.json') != '' || hashFiles('yarn.lock') != '' || hashFiles('pnpm-lock.yaml') != '') && 'npm' || '' }}
      
      - name: Install Dependencies
        run: |
          if [ -f "package-lock.json" ]; then
            npm ci
          elif [ -f "package.json" ]; then
            npm install
          fi
        shell: bash
      
      - name: Generate Changelog
        uses: ravitejakamalapuram/.github-workflows-shared/composite-actions/common/changelog@v1
        with:
          output-file: 'CHANGELOG.md'
          format: 'markdown'
      
      - name: Bump Version
        id: version
        uses: ravitejakamalapuram/.github-workflows-shared/composite-actions/common/version-bump@v1
        with:
          bump-type: 'auto'
          tag-prefix: 'v'
          push-tag: 'true'
      
      - name: Build Extension
        if: inputs.build-command != ''
        env:
          BUILD_COMMAND: ${{ inputs.build-command }}
        run: bash -euo pipefail -c "$BUILD_COMMAND"
        shell: bash

      - name: Update manifest.json Version
        env:
          EXTENSION_DIR: ${{ inputs.extension-dir }}
        run: |
          VERSION="${{ steps.version.outputs.new-version }}"
          if command -v jq >/dev/null 2>&1; then
            jq --arg version "$VERSION" '.version = $version' "$EXTENSION_DIR/manifest.json" > temp.json
            mv temp.json "$EXTENSION_DIR/manifest.json"
          else
            python3 -c "import json, os; ext_dir=os.getenv('EXTENSION_DIR', '.'); d=json.load(open(f'{ext_dir}/manifest.json')); d['version']='$VERSION'; json.dump(d, open(f'{ext_dir}/manifest.json', 'w'), indent=2)"
          fi
        shell: bash
      
      - name: Package Extension
        id: package
        uses: ravitejakamalapuram/.github-workflows-shared/composite-actions/chrome-extension/package@v1
        with:
          extension-dir: ${{ inputs.extension-dir }}
          output-name: 'extension'
          include-source-maps: 'false'
      
      - name: Create GitHub Release
        uses: ravitejakamalapuram/.github-workflows-shared/composite-actions/common/create-release@v1
        with:
          tag-name: ${{ steps.version.outputs.new-tag }}
          release-name: 'Release ${{ steps.version.outputs.new-tag }}'
          body-file: 'CHANGELOG.md'
          files: ${{ steps.package.outputs.package-path }}
      
      - name: Publish to Chrome Web Store
        id: publish
        uses: ravitejakamalapuram/.github-workflows-shared/composite-actions/chrome-extension/publish-cws@v1
        with:
          extension-id: ${{ secrets.chrome-extension-id }}
          client-id: ${{ secrets.chrome-client-id }}
          client-secret: ${{ secrets.chrome-client-secret }}
          refresh-token: ${{ secrets.chrome-refresh-token }}
          zip-path: ${{ steps.package.outputs.package-path }}
          publish-target: ${{ inputs.publish-target }}
          auto-publish: ${{ inputs.auto-publish }}

      - name: Commit and Push Updates
        if: success()
        env:
          EXTENSION_DIR: ${{ inputs.extension-dir }}
        run: |
          if [ -f "app-metadata.json" ]; then
            FILE="app-metadata.json"
          elif [ -f ".app-metadata.json" ]; then
            FILE=".app-metadata.json"
          else
            FILE=""
          fi

          if [ -n "$FILE" ]; then
            jq --arg ext_dir "$EXTENSION_DIR" --arg store_id "${{ secrets.chrome-extension-id }}" '
              if .modules then
                .modules = [
                  .modules[] |
                  if .type == "chrome-extension" and ($ext_dir == "" or $ext_dir == "." or .path == $ext_dir) then
                    .status = "published" |
                    if $store_id != "" then
                      .storeId = $store_id |
                      .storeUrl = "https://chromewebstore.google.com/detail/\($store_id)"
                    else . end
                  else . end
                ]
              else . end
            ' "$FILE" > temp.json && mv temp.json "$FILE"
            echo "✅ Updated app-metadata.json status to published"
          fi
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Add each path on its own: one missing or gitignored path (e.g. a built
          # dist/manifest.json) must not stop the others from being committed.
          for f in app-metadata.json .app-metadata.json "$EXTENSION_DIR/manifest.json" CHANGELOG.md; do
            if [ -e "$f" ] && ! git check-ignore -q "$f"; then git add "$f"; fi
          done
          git commit -m "chore(release): update manifest version and metadata to published [skip ci]" || true
          git pull --rebase --autostash origin main
          git push origin HEAD:main
        shell: bash
